const form = document.getElementById("registerForm");const checkbox = document.getElementById("conditions_input");const container = document.querySelector(".checkbox-container");const mdpInput = document.getElementById("mdp_input");form.addEventListener("submit", function (e) {  if (!checkbox.checked) {    e.preventDefault();    container.classList.add("shake");    setTimeout(() => {      container.classList.remove("shake");    }, 300);  }     // Vérification de la longueur du mdp  if (mdpInput.value.length < 8) {    e.preventDefault(); // Empêche l'envoi du formulaire    alert("Le mot de passe doit contenir au minimum 8 caractères !");    mdpInput.style.border = "1px solid #b30000";  }});function showRegister() {  const login = document.getElementById("login-section");  const register = document.getElementById("register-section");  login.classList.add("hidden");  login.classList.remove("fade-in"); // On retire l'anim précédente  register.classList.remove("hidden");  // On ajoute un léger délai pour forcer le navigateur à détecter le changement  setTimeout(() => {    register.classList.add("fade-in");  }, 10);}   function showLogin() {  const login = document.getElementById("login-section");  const register = document.getElementById("register-section");  register.classList.add("hidden");  register.classList.remove("fade-in");  login.classList.remove("hidden");  setTimeout(() => {    login.classList.add("fade-in");  }, 10);}